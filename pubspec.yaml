name: news_client
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.16.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.1 # Обновление до последней совместимой версии
  path_provider: ^2.0.11
  sqflite: ^2.0.2+1
  url_launcher: ^6.0.20
  google_fonts: ^6.2.1 # Обновление до последней версии
  flutter_staggered_grid_view: ^0.7.0
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/background.jpg
